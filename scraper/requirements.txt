requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
anthropic>=0.40.0
supabase>=2.4.0
python-dotenv>=1.0.0

# autofill/ only — not needed by the scraper's own scheduled runs (GitHub
# Actions/Modal), only for the local, manually-invoked autofill.py tool.
playwright>=1.61.0
pyyaml>=6.0.0
